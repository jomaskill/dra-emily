---
phase: 01-baseline-content-freeze-approval-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/Console/Commands/GenerateGovernanceInventory.php
  - app/Governance/GovernanceInventory.php
  - .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
  - .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.md
  - .planning/phases/01-baseline-content-freeze-approval-gates/01-BASELINE.md
  - tests/Feature/Governance/PublicSurfaceInventoryTest.php
  - tests/Unit/Governance/BaselineRecordTest.php
autonomous: true
requirements:
  - GOV-01
  - MEAS-03
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01 / GOV-01: The inventory expands the live Laravel route collection, including every current procedure and article slug, the article index, sitemap, and invalid parameter behavior, instead of trusting an obsolete hand-written route list."
    - "D-01: Every conversion path, visible or machine-readable string, metadata value, JSON-LD property, sitemap value, analytics surface, public media source/derivative, testimonial, and configured assertion has a dated, revision-bound inventory identity and review state."
    - "D-01: The baseline records exact revision and dirty-tree state before any later public change, plus accessibility, performance, and measurement observations for the homepage, generic procedure, Full Face, current route union, and sitemap."
    - "UI/empty: Zero discovered FAQ, procedure, media, or baseline items is reported explicitly; a zero count for any required surface or unavailable evidence uses the governance empty-state message and blocks completeness."
    - statement: "UI/loading: Loaded and throttled or in-flight media, external-font, navigation, and CTA behavior is evidenced on representative pages, including whether animation can leave core content invisible."
      verification: backstop
    - "UI/error: Actual failures for media, fonts, scripts, routes, external links, and capture tools retain the raw error and governance error message without inventing approval or a public fallback."
    - "UI/populated: The normal current render is inventoried for the homepage, generic procedure, Full Face, shared shell, JSON-LD, and sitemap."
    - "UI/partial: Evidence reports retain known fields, mark every absent, stale, or conflicting field, and keep the affected item and release gate blocked."
    - statement: "UI/overflow: At 320px, 768px, 1440px, and 200% zoom, explicit evidence records wrapping, clipping, horizontal scrolling, overlap, and truncation without remediation."
      verification: backstop
    - "UI/zero-one-many: Procedure, FAQ, media, CTA, JSON-LD, and sitemap collections report explicit cardinality and correct singular/plural meaning; a zero required collection fails completeness."
    - statement: "UI/long-text: The longest current Portuguese headings, CTA labels, navigation, breadcrumbs, FAQ answers, footer text, and evidence-table values have viewport-specific wrap, reflow, and clipping evidence without shortened public copy."
      verification: backstop
    - statement: "UI/reduced-motion: Every representative template has direct `prefers-reduced-motion: reduce` evidence for fade, scale, smooth-scroll, hover-transform, and disclosure behavior; continuing motion or hidden content is recorded as a dated finding."
      verification: backstop
    - "UI/keyboard-focus: Each representative page records the complete keyboard order, accessible name, focus visibility, activation behavior, expanded state, and destination for navigation, CTA, card, FAQ, and footer controls."
    - "UI/unavailable-measurement: An unavailable production environment, external app, browser, or tool is recorded as `not measured` with reason, date, and blocking owner; absence of evidence never becomes a pass."
    - "UI/machine-readable-parity: JSON-LD, FAQ schema, sitemap, and metadata are parsed independently and every value maps to visible content or is labelled machine-readable-only for exact-context review."
  artifacts:
    - path: .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
      provides: "Deterministic machine-readable inventory"
    - path: .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.md
      provides: "Human-reviewable inventory and freeze manifest"
    - path: .planning/phases/01-baseline-content-freeze-approval-gates/01-BASELINE.md
      provides: "Append-only dated accessibility, performance, and measurement baseline"
    - path: tests/Feature/Governance/PublicSurfaceInventoryTest.php
      provides: "Wave 0 coverage of the current route/config/rendered publication surface"
    - path: tests/Unit/Governance/BaselineRecordTest.php
      provides: "Baseline schema, viewport, evidence, and metric-language contract"
  key_links:
    - from: app/Governance/GovernanceInventory.php
      to: routes/web.php
      via: "Laravel route collection plus config-backed parameter expansion"
      pattern: "getRoutes"
    - from: .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
      to: .planning/phases/01-baseline-content-freeze-approval-gates/01-BASELINE.md
      via: "exact revision, route, stable ID, content hash, and evidence reference"
      pattern: "content_hash"
  prohibitions:
    - requirement: GOV-01
      category: safety
      status: unverified
      flagged: true
      statement: "Phase 1 inventory or baseline work MUST NOT change public copy, DOM, CSS, JavaScript, assets, routes, metadata, structured data, sitemap, CTA destinations, analytics, storage, or network behavior."
    - requirement: GOV-01
      category: transparency
      status: unverified
      flagged: true
      statement: "A missing capture or unavailable measurement MUST NOT be labelled passing, equivalent to another viewport/template, or complete on the strength of source inspection alone."
    - requirement: MEAS-03
      category: transparency
      status: unverified
      flagged: true
      statement: "A WhatsApp link activation MUST NOT be described as a message, conversation, appointment, attendance, clinical eligibility decision, patient, qualified lead, or revenue event."
---

## Phase Goal

**As a** site owner, **I want to** know exactly what the site publishes and hold dated decision evidence, **so that** I can release only content that is safe and approved.

<objective>
Expand the tracer into the complete current publication inventory and capture the dated, non-remediating visual/accessibility/performance/measurement baseline.

Purpose: Freeze what exists before later phases change public behavior.
Output: Canonical JSON/Markdown inventory, baseline report, complete route/render coverage, and baseline contract tests.
</objective>

<execution_context>
@/Users/jomas/.codex/gsd-core/workflows/execute-plan.md
@/Users/jomas/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-baseline-content-freeze-approval-gates/01-01-SUMMARY.md
@.planning/phases/01-baseline-content-freeze-approval-gates/01-UI-SPEC.md
@.planning/phases/01-baseline-content-freeze-approval-gates/01-PATTERNS.md
@routes/web.php
@config/clinic.php
@config/procedures.php
@config/articles.php
@config/faq.php
@resources/views/components/site-layout.blade.php
@resources/views/partials/schema.blade.php
@resources/views/partials/procedure-schema.blade.php
@resources/views/partials/article-schema.blade.php
@resources/views/sitemap.blade.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand deterministic inventory across every current publication surface</name>
  <read_first>
    AGENTS.md
    .claude/skills/laravel-best-practices/SKILL.md
    .claude/skills/pest-testing/SKILL.md
    app/Governance/GovernanceInventory.php
    app/Console/Commands/GenerateGovernanceInventory.php
    routes/web.php
    app/Http/Controllers/ProcedureController.php
    app/Http/Controllers/ArticleController.php
    tests/Feature/SiteStructureTest.php
    tests/Feature/ArticleTest.php
  </read_first>
  <files>app/Governance/GovernanceInventory.php, app/Console/Commands/GenerateGovernanceInventory.php, .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json, .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.md, tests/Feature/Governance/PublicSurfaceInventoryTest.php</files>
  <behavior>
    - Every named public GET/HEAD route is inventoried; config-backed procedure/article parameters expand from their current keys and an unresolved parameterized route is a blocking scan error.
    - Every rendered HTML route records metadata, headings/text candidates, links and conversion destinations, media contexts, JSON-LD blocks/properties, and visible versus machine-readable status; sitemap XML is parsed independently.
    - Every relevant config/Blade/public-asset source is cross-referenced, and both discovered-to-inventory and inventory-to-live-surface completeness are checked.
    - Same-tree reruns preserve byte-equivalent ordered item data and never modify evidence registers.
  </behavior>
  <action>
    Before editing, call Laravel Boost `search-docs` for `laravel/framework` and `pestphp/pest` using `route collection route parameters`, `http kernel request console command`, and `pest response json xml parsing`. Create the feature test with `php artisan make:test --pest Governance/PublicSurfaceInventoryTest --no-interaction` and write the failing coverage first. Expand the live route collection, including `/`, `/artigos`, every current `/artigos/{slug}`, every current `/procedimentos/{slug}`, and `/sitemap.xml`; assert invalid procedure and article slugs are 404. Enumerate known configuration fields, relevant Blade sources, public media and derivatives, built asset/analytics declarations, rendered visible text candidates, metadata, all link destinations, JSON-LD, and sitemap values. Source enumeration supplements rendered parsing; it never substitutes for it. Treat every generated candidate as an observation requiring human classification, not a claim conclusion. Write the canonical outputs to the Phase 1 directory with exact run metadata and explicit omissions/errors. Never edit routes, configs under observation, views, CSS, JavaScript, public assets, or evidence registers.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty --format agent &amp;&amp; php artisan test --compact --filter=PublicSurfaceInventory &amp;&amp; php artisan governance:inventory</automated>
  </verify>
  <acceptance_criteria>
    Both canonical inventory files are non-empty, parseable, deduplicated, tied to the exact revision/dirty tree, include every live route and required publication category in both directions, and identify all scan errors without exposing sensitive values.
  </acceptance_criteria>
  <done>The owner has a complete deterministic inventory of the current public site, including the newer article surfaces absent from the older research snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture the append-only accessibility, performance, and measurement baseline</name>
  <read_first>
    .planning/phases/01-baseline-content-freeze-approval-gates/01-UI-SPEC.md
    .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
    app/Governance/GovernanceEvidence.php
    resources/css/app.css
    resources/js/app.js
    package.json
    public/build/manifest.json
  </read_first>
  <files>.planning/phases/01-baseline-content-freeze-approval-gates/01-BASELINE.md, tests/Unit/Governance/BaselineRecordTest.php</files>
  <behavior>
    - The baseline has an append-only run with offset-aware timestamp, exact SHA, dirty-tree description, environment/base URL, browser/OS/tool versions, and evidence reference for every required matrix record.
    - Homepage, one generic procedure, Full Face, every public route, invalid slugs, navigation, footer, CTAs, FAQs, media, JSON-LD, sitemap, and analytics/network/storage behavior are covered.
    - 320px, 768px, 1440px, 200% zoom, keyboard traversal, reduced motion, loaded/failure, and responsive overflow/long-copy states have direct evidence or an explicit `not measured` reason/date/owner blocker.
    - Measurement language distinguishes configured analytics from observed collection and distinguishes a WhatsApp click from every downstream clinic outcome.
  </behavior>
  <action>
    Use Laravel Boost `search-docs` for `pestphp/pest` with `unit test datasets json parsing` before creating `php artisan make:test --pest --unit Governance/BaselineRecordTest --no-interaction`. Use Laravel Boost `get-absolute-url` to resolve the Herd base URL before recording it. Populate `01-BASELINE.md` exactly from the UI-SPEC capture matrix, using browser screenshots/traces/computed-style/accessibility output when available and an opaque controlled evidence locator in the record. Do not send WhatsApp messages, trigger production analytics, or copy URLs containing sensitive parameters. Record failures and unavailable tools/environments as blocked `not measured` entries with the required owner; do not remediate observations in this phase. The test validates record shape, required viewports/states, append-only run identities, evidence-or-blocker completeness, and banned metric conflation.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty --format agent &amp;&amp; php artisan test --compact --filter=BaselineRecord &amp;&amp; php artisan test --compact --filter=PublicSurfaceInventory</automated>
    <human-check>At end-of-phase review, compare the baseline evidence locators to the exact revision and confirm screenshots/traces or explicit `not measured` blockers exist for all four UI backstop truths.</human-check>
  </verify>
  <acceptance_criteria>
    The dated baseline covers every matrix row and required viewport/state with direct evidence or an honest blocker, preserves observed failures, and makes no accessibility, performance, analytics, content, or design change.
  </acceptance_criteria>
  <done>The exact pre-change public experience has a reviewable, repeatable baseline rather than an inferred pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository/config/rendered response → inventory | Potentially divergent sources are reconciled into exact publication observations. |
| browser/deployment → baseline report | Environment-specific evidence enters a repository report through opaque, non-sensitive locators. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information Disclosure | Inventory roots and link/network capture | high | mitigate | Restrict roots and sanitize destinations/output; exclude environment, runtime, secrets, patient data, and complete sensitive URLs. |
| T-01-07 | Tampering | Inventory completeness | high | mitigate | Derive routes/config keys live, perform bidirectional checks, parse rendered JSON-LD/XML, and fail on unresolved parameter routes. |
| T-01-08 | Repudiation | Baseline provenance | high | mitigate | Require exact SHA, dirty-tree description, environment/tool/browser metadata, timestamps, and immutable run IDs. |
| T-01-09 | Spoofing | Unavailable evidence | medium | mitigate | Require direct evidence locator or `not measured` reason/date/owner; source inspection alone cannot claim pass. |
| T-01-10 | Tampering | Public behavior freeze | high | mitigate | Scope writes to governance code/tests/artifacts and verify protected source plus rendered output remains unchanged in Plan 05. |
</threat_model>

<verification>
Run `vendor/bin/pint --dirty --format agent` before both focused tests and `php artisan governance:inventory`, confirm the canonical JSON parses, inspect every UI consideration row in `01-BASELINE.md`, and confirm all public application files remain untouched. Plan 05 must compare the before capture to the exact final candidate across rendered DOM, copy, style, assets, interactions, network/storage behavior, structured data, sitemap, and analytics; any difference, missing category, revision mismatch, or unavailable comparison remains blocking.
</verification>

<success_criteria>
- Every current public route and configured slug is represented, including article routes.
- Visible, machine-readable, conversion, media, analytics, and sitemap observations are complete and cross-referenced.
- Every resolved UI consideration is represented as an explicit or structured backstop truth.
- Baseline evidence is revision-bound and honestly records failures or missing measurements.
- No public behavior or analytics behavior changes.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json` — deterministic inventory envelope and publication-item array.
- `.planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.md` — reviewable release inventory and freeze manifest.
- `.planning/phases/01-baseline-content-freeze-approval-gates/01-BASELINE.md` — append-only baseline runs and evidence/blocker records.
- `tests/Feature/Governance/PublicSurfaceInventoryTest.php` — complete public-surface and bidirectional inventory coverage.
- `tests/Unit/Governance/BaselineRecordTest.php` — baseline/UI-state/metric-language contract.
- `GovernanceInventory` gains full `scan`, route expansion, rendered extraction, source enumeration, deterministic sort, and canonical write behavior.

<output>
Create `.planning/phases/01-baseline-content-freeze-approval-gates/01-02-SUMMARY.md` when done.
</output>
