---
phase: 01-baseline-content-freeze-approval-gates
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - app/Governance/ClaimRegister.php
  - app/Governance/MediaRegister.php
  - .planning/phases/01-baseline-content-freeze-approval-gates/01-CLAIMS.md
  - .planning/phases/01-baseline-content-freeze-approval-gates/01-MEDIA.md
  - tests/Unit/Governance/ClaimRegisterTest.php
  - tests/Unit/Governance/MediaRegisterTest.php
autonomous: true
requirements:
  - GOV-03
  - GOV-04
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02 / GOV-03: Every discovered clinical-claim candidate is joined to an exact text/hash/locale/route/context record with source, owner, last-reviewed date, Dra. Emily evidence reference, human decision/date, and derived disposition."
    - "Edge GOV-03/empty: discovered clinical claims plus an empty or missing claim register blocks; no-claim status is valid only when inventory evidence explicitly proves zero candidates and the reviewer records that decision."
    - "Edge GOV-03/encoding: clinical approval binds to the exact captured UTF-8 bytes, SHA-256 hash, `pt-BR` locale, and each publication context; semantic similarity does not transfer approval."
    - "Edge GOV-03/idempotency: repeated inventory or validation runs preserve the human claim record byte-for-byte and cannot duplicate, create, or upgrade its decision."
    - "Edge GOV-03/concurrency: scanner and validator access to claims is read-only; a concurrent generated-output write cannot mutate or replace `01-CLAIMS.md`."
    - "D-03 / GOV-04: Every public asset, derivative, rendered media context, and testimonial has a stable item record; patient/unknown items require opaque provenance and written-authorization references, responsible-professional attribution, and current exact-context decision."
    - "Edge GOV-04/adjacency: one asset or testimonial reused in multiple contexts retains one stable source identity and distinct context entries; an approval for one context never merges into another."
    - "Edge GOV-04/empty: any public media/testimonial inventory with a missing or empty review register blocks; zero patient items is not inferred from filenames or an empty table."
    - "Edge GOV-04/encoding: asset hashes use exact file bytes and testimonial hashes use exact UTF-8 bytes; paths are repository-relative POSIX strings and locale/context remain explicit."
    - "Edge GOV-04/ordering: media records sort deterministically by stable asset/text ID and context; duplicate full identities and conflicting classifications block."
    - "Edge GOV-04/idempotency: repeated scans and checks neither append duplicate media records nor alter classification, references, attribution, or human decision."
    - "Edge GOV-04/concurrency: generated inventory writes and media-register review are isolated; concurrent automation cannot write or overwrite the human media register."
  artifacts:
    - path: .planning/phases/01-baseline-content-freeze-approval-gates/01-CLAIMS.md
      provides: "Human-maintained exact-context clinical claim register"
    - path: .planning/phases/01-baseline-content-freeze-approval-gates/01-MEDIA.md
      provides: "Human-maintained patient-media/testimonial register with opaque evidence references"
    - path: app/Governance/ClaimRegister.php
      provides: "Strict GOV-03 validator"
      contains: "class ClaimRegister"
    - path: app/Governance/MediaRegister.php
      provides: "Strict GOV-04 validator"
      contains: "class MediaRegister"
  key_links:
    - from: .planning/phases/01-baseline-content-freeze-approval-gates/01-CLAIMS.md
      to: .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
      via: "stable ID + exact hash + locale + route/context"
      pattern: "content_hash"
    - from: .planning/phases/01-baseline-content-freeze-approval-gates/01-MEDIA.md
      to: .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
      via: "asset/testimonial identity and all rendered contexts"
      pattern: "stable_id"
  prohibitions:
    - requirement: GOV-03
      category: safety
      status: unverified
      flagged: true
      statement: "Automation MUST NOT assert a clinical conclusion, decide whether wording is safe, treat a content source as clinical approval, or write an `approved` claim decision for Dra. Emily."
    - requirement: GOV-04
      category: privacy
      status: unverified
      flagged: true
      statement: "Patient identity, health or treatment details, consent scans, signatures, legal correspondence, credentials, and complete controlled evidence MUST NOT enter Git, screenshots, test fixtures, logs, or command output; only opaque off-repository locators may be stored."
    - requirement: GOV-04
      category: transparency
      status: unverified
      flagged: true
      statement: "A filename, image subject guess, testimonial placement, metadata field, or prior use MUST NOT be treated as proof of patient status, provenance, authorization, attribution, or permission for the current publishing context."
---

## Phase Goal

**As a** site owner, **I want to** know exactly what the site publishes and hold dated decision evidence, **so that** I can release only content that is safe and approved.

<objective>
Create the clinical-claim and patient-media evidence registers and validators without letting automation perform human adjudication.

Purpose: Make clinical and patient-evidence gaps explicit, exact-context, and fail closed.
Output: Two human-maintained registers, two strict validators, and the corresponding Wave 0 Pest tests.
</objective>

<execution_context>
@/Users/jomas/.codex/gsd-core/workflows/execute-plan.md
@/Users/jomas/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-baseline-content-freeze-approval-gates/01-02-SUMMARY.md
@.planning/phases/01-baseline-content-freeze-approval-gates/01-CONTEXT.md
@.planning/phases/01-baseline-content-freeze-approval-gates/01-RESEARCH.md
@.planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
@config/governance.php
@app/Governance/GovernanceEvidence.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed pending clinical candidates and enforce Dra. Emily's exact-context claim decision</name>
  <read_first>
    AGENTS.md
    .claude/skills/laravel-best-practices/SKILL.md
    .claude/skills/pest-testing/SKILL.md
    app/Governance/GovernanceEvidence.php
    config/governance.php
    .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
    tests/Feature/MedicalContentTest.php
    tests/Feature/HomeSchemaTest.php
  </read_first>
  <files>app/Governance/ClaimRegister.php, .planning/phases/01-baseline-content-freeze-approval-gates/01-CLAIMS.md, tests/Unit/Governance/ClaimRegisterTest.php</files>
  <behavior>
    - Every candidate from visible copy, metadata, FAQ, JSON-LD, article/procedure config, and machine-readable-only output resolves to a record or an explicit human non-claim classification.
    - Missing source, owner, review date, exact identity, Dra. Emily evidence reference, reviewer/date, or current decision blocks the item.
    - RED empty fixtures: a non-empty discovered-candidate list plus missing or zero-entry register returns `missing_claim_register` or `unregistered_claim`; an inventory that proves zero candidates passes only with an exact-revision human `no_claim_candidates` record containing reviewer, date, and reason.
    - RED adjacency/encoding fixtures: identical text in visible and JSON-LD contexts requires two context bindings, and NFC/NFD or whitespace-different UTF-8 byte sequences have distinct SHA-256 identities; approval of one never approves the other.
    - RED status/conflict fixtures: pending, rejected, quarantined, expired, stale-hash, context mismatch, duplicate full identity, conflicting decisions for one identity, and an unregistered candidate each fail with the expected stable item ID and reason code.
    - RED ordering fixture: reverse and randomly shuffle the same valid records and assert the derived findings remain ordered by stable ID, route, context, then reason; input order cannot change output bytes.
    - RED idempotency/read-concurrency fixture: run validation twice and through two simultaneously held shared-read snapshots, assert identical findings and an unchanged SHA-256 checksum for `01-CLAIMS.md`; generated processes have no path that authors or upgrades the human decision.
  </behavior>
  <action>
    Before editing Laravel/Pest code, call Laravel Boost `search-docs` for `laravel/framework` and `pestphp/pest` with `service class configuration`, `json exceptions date validation`, and `pest datasets`. Create the class and unit test using `php artisan make:class Governance/ClaimRegister --no-interaction` and `php artisan make:test --pest --unit Governance/ClaimRegisterTest --no-interaction`; write every RED fixture named above before implementation. Seed `01-CLAIMS.md` from inventory candidates with `pending` status only, then treat it as human-maintained and never regenerate or overwrite it. Its single tagged JSON payload records stable ID, exact text/hash, locale, every route/context, source locator, owner, last-reviewed date, Dra. Emily evidence locator, reviewer, decision date, decision, and derived disposition inputs. Permit an explicit human `not_clinical_claim` classification only with reviewer/date/reason tied to the exact item; automation cannot assign it. Sort findings by stable ID, route, context, and reason; reject duplicate full identities and conflicting decisions. Acquire shared locks for snapshot reads, keep every validator path read-only, and test the register checksum before and after repeat/shared-reader runs. The validator returns structured IDs/reasons and never claim text or evidence contents.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty --format agent &amp;&amp; php artisan test --compact --filter=ClaimRegister</automated>
  </verify>
  <acceptance_criteria>
    Every current claim candidate is present as pending or explicitly human-classified, all GOV-03 fields and exact contexts are validated, incomplete/non-current decisions block, and no automated writer can mark clinical approval.
  </acceptance_criteria>
  <done>The clinical release set is reviewable by Dra. Emily and remains blocked wherever her exact-current decision is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify all media as pending and enforce off-repository authorization evidence</name>
  <read_first>
    AGENTS.md
    .claude/skills/laravel-best-practices/SKILL.md
    .claude/skills/pest-testing/SKILL.md
    app/Governance/GovernanceEvidence.php
    config/governance.php
    .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
    resources/views/components/picture.blade.php
    tests/Feature/ResponsiveImageTest.php
    tests/Feature/SiteStructureTest.php
  </read_first>
  <files>app/Governance/MediaRegister.php, .planning/phases/01-baseline-content-freeze-approval-gates/01-MEDIA.md, tests/Unit/Governance/MediaRegisterTest.php</files>
  <behavior>
    - Every public source file, derivative, rendered image context, before/after item, and testimonial resolves to one stable record with all contexts.
    - Classification is exactly patient, clinician/professional, stock/illustrative, or unknown; unknown and patient records block without opaque provenance/authorization references, attribution, exact-context decision, reviewer, and date.
    - RED empty fixture: any discovered public media/testimonial identity plus a missing or empty register returns `missing_media_register` or `unregistered_media`; zero patient items is accepted only after an authorized exact-revision classification covers every discovered item.
    - RED adjacency/encoding fixtures: one asset reused in hero and schema contexts remains one source identity with two required context entries, while byte-different assets and UTF-8-different testimonial text retain distinct hashes and cannot inherit a decision.
    - RED privacy/conflict fixtures: sensitive keys, credential-bearing evidence locators, patient-shaped identifiers, embedded evidence, paths outside the repository allow-list, context mismatch, duplicate full identities, conflicting classifications, and conflicting decisions each fail with the expected stable ID/reason and never echo the rejected value.
    - RED ordering fixture: reversed and shuffled records produce findings ordered by stable asset/text ID, route/context, then reason; source order cannot affect output bytes.
    - RED idempotency/read-concurrency fixture: repeat validation and validate from two simultaneously held shared-read snapshots, then assert byte-identical findings and an unchanged SHA-256 checksum for `01-MEDIA.md`; generated inventory work cannot mutate the register.
  </behavior>
  <action>
    Search version-scoped Laravel and Pest docs first with `filesystem paths`, `json validation`, and `pest datasets`, then create `MediaRegister` and its unit test with the appropriate `php artisan make:` commands and `--no-interaction`; create every RED fixture named above before production logic. Seed one record for every inventoried media/testimonial identity and rendering context with `unknown` classification and `pending` decision unless a human-maintained record already exists; never infer status from filename, alt text, subject appearance, or prior publication. Keep only opaque controlled-system locators and minimum governance metadata. Enforce exact file/text hash, source/derivative relationships, all contexts, classification, responsible professional, decision/date, expiry/recheck fields, bidirectional completeness, duplicate/conflict rejection, and deterministic stable-ID/context/reason ordering. Read a shared-locked snapshot and prove repeat/shared-reader runs preserve the human file checksum. Return only stable IDs and reason codes on failure.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty --format agent &amp;&amp; php artisan test --compact --filter=MediaRegister</automated>
  </verify>
  <acceptance_criteria>
    Every current public media/testimonial item is represented, unknown/patient content blocks without complete opaque evidence and exact-context approval, sensitive evidence is rejected and never echoed, and repeated scans preserve human records.
  </acceptance_criteria>
  <done>The patient-media release set is complete, privacy-safe, context-specific, and fail closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public claim/media observation → human classification | Repository evidence does not establish clinical truth or patient authorization. |
| controlled clinical/consent systems → repository register | Only opaque locators and minimum non-sensitive governance metadata may cross. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Spoofing | Dra. Emily approval | high | mitigate | Require named reviewer/date/evidence locator and exact item identity; no automated approval writer exists. |
| T-01-12 | Information Disclosure | Media and clinical evidence | high | mitigate | Reject sensitive keys/payload patterns and emit IDs/reason codes only; repository stores opaque locators. |
| T-01-13 | Tampering | Exact-context reuse | high | mitigate | Bind each record to exact hash, locale, route, and context; block mismatches, duplicates, and conflicts. |
| T-01-14 | Repudiation | Human classifications | medium | mitigate | Require reviewer, decision date, evidence locator, and explicit reason for non-claim/non-patient classifications. |
</threat_model>

<verification>
Run `vendor/bin/pint --dirty --format agent` before both focused unit suites, then inspect that current inventory candidates appear in the two registers only as pending/unknown until reviewed. Confirm the generated commands never target either register for writing.
</verification>

<success_criteria>
- D-02/GOV-03 and D-03/GOV-04 fields are complete and tied to exact identities.
- All ten claim/media edge-probe cases are explicit and tested.
- Sensitive evidence never enters repository artifacts or diagnostics.
- Every missing, stale, conflicting, unknown, or non-approved record remains blocked.
</success_criteria>

## Artifacts this phase produces

- `app/Governance/ClaimRegister.php` — `ClaimRegister` with exact-context GOV-03 validation and structured findings.
- `.planning/phases/01-baseline-content-freeze-approval-gates/01-CLAIMS.md` — human-maintained clinical claim records.
- `tests/Unit/Governance/ClaimRegisterTest.php` — clinical schema, edge, and fail-closed coverage.
- `app/Governance/MediaRegister.php` — `MediaRegister` with context/evidence/privacy validation.
- `.planning/phases/01-baseline-content-freeze-approval-gates/01-MEDIA.md` — human-maintained media/testimonial records.
- `tests/Unit/Governance/MediaRegisterTest.php` — media schema, sensitive-data, edge, and fail-closed coverage.

<output>
Create `.planning/phases/01-baseline-content-freeze-approval-gates/01-03-SUMMARY.md` when done.
</output>
