---
phase: 01-baseline-content-freeze-approval-gates
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - app/Governance/ProfessionalWordingRegister.php
  - app/Governance/OperationsRegister.php
  - app/Governance/PrivacyDecision.php
  - .planning/phases/01-baseline-content-freeze-approval-gates/01-PROFESSIONAL-WORDING.md
  - .planning/phases/01-baseline-content-freeze-approval-gates/01-OPERATIONS.md
  - .planning/phases/01-baseline-content-freeze-approval-gates/01-PRIVACY-DECISION.md
  - tests/Unit/Governance/ProfessionalWordingRegisterTest.php
  - tests/Unit/Governance/OperationsRegisterTest.php
  - tests/Unit/Governance/PrivacyDecisionTest.php
autonomous: true
requirements:
  - GOV-05
  - GOV-06
  - MEAS-03
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-04 / GOV-05: Every identity, title, registration, qualification, specialty, advertising, and HOF phrase is exact-context inventory; scope-sensitive wording blocks without current registration evidence and dated CRO-MG or qualified legal decision."
    - "Edge GOV-05/empty: an absent wording record, missing current professional evidence, or missing scope-sensitive reviewer decision blocks; an empty register cannot prove no sensitive wording exists."
    - "Edge GOV-05/encoding: wording approval binds to exact UTF-8 bytes/hash, `pt-BR` locale, and each visible/schema/metadata context; normalized or similar wording receives no inherited decision."
    - "Edge GOV-05/idempotency: repeated scans/checks preserve professional/legal decisions and evidence locators and return the same derived findings for the same inputs."
    - "Edge GOV-05/concurrency: automation reads but never writes the professional/legal decision; concurrent inventory output cannot replace the human register."
    - "D-05 / GOV-06: Identity, address, contact, hours, directions, accessibility, WhatsApp operations, escalation, response, and follow-up promises each record configured value separately from observed value, verifier/date, evidence reference, status, and recheck date."
    - "Edge GOV-06/adjacency: configured and observed values remain distinct fields even when textually equal, and one operational fact reused in multiple contexts retains every context rather than merging evidence."
    - "Edge GOV-06/empty: every required operational domain is present; missing, blank, stale, or unmeasured domains block instead of inheriting config defaults."
    - "Edge GOV-06/encoding: operational strings and URLs retain exact UTF-8 values and source/context identity; any lossy normalization or semantically ambiguous comparison blocks."
    - "Edge GOV-06/ordering: required operational domains use a canonical order, extra records use stable-ID order, and duplicates or conflicting current observations fail."
    - "Edge GOV-06/idempotency: a repeated gate run does not edit observations and yields identical status for identical register/inventory inputs."
    - "Edge GOV-06/concurrency: inventory and gate commands cannot write operations decisions; concurrent readers derive the same status from the same exact revision."
    - "D-06 / MEAS-03: The privacy record names controller/processors, purpose, parameters/data, lawful basis, consent trigger and deny/revoke behavior, retention, access, transfers, exact production configuration, approver/date, decision, and unresolved questions."
    - "Edge MEAS-03/empty: a missing or empty privacy record or any blank required field blocks analytics and release; a configured GA4 identifier is neither observed collection nor approval."
    - "Edge MEAS-03/encoding: production analytics/consent configuration is bound by exact UTF-8 values and a content hash without storing patient identifiers, message content, or health data."
    - "Edge MEAS-03/idempotency: inventory/privacy checks never enable analytics, inject scripts, mutate consent behavior, or edit the human privacy decision."
    - "Edge MEAS-03/concurrency: concurrent scans are read-only over the privacy record and all derive analytics-disabled status until the exact production decision is explicitly approved."
  artifacts:
    - path: .planning/phases/01-baseline-content-freeze-approval-gates/01-PROFESSIONAL-WORDING.md
      provides: "Current professional/HOF wording review register"
    - path: .planning/phases/01-baseline-content-freeze-approval-gates/01-OPERATIONS.md
      provides: "Current clinic operations verification register"
    - path: .planning/phases/01-baseline-content-freeze-approval-gates/01-PRIVACY-DECISION.md
      provides: "LGPD, consent, and production analytics decision"
    - path: app/Governance/ProfessionalWordingRegister.php
      provides: "GOV-05 validator"
    - path: app/Governance/OperationsRegister.php
      provides: "GOV-06 validator"
    - path: app/Governance/PrivacyDecision.php
      provides: "MEAS-03 validator"
  key_links:
    - from: .planning/phases/01-baseline-content-freeze-approval-gates/01-PROFESSIONAL-WORDING.md
      to: .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
      via: "exact wording/hash and every visible or machine-readable context"
      pattern: "scope_sensitive"
    - from: .planning/phases/01-baseline-content-freeze-approval-gates/01-OPERATIONS.md
      to: config/clinic.php
      via: "configured assertion recorded separately from human-observed value"
      pattern: "configured_value"
    - from: .planning/phases/01-baseline-content-freeze-approval-gates/01-PRIVACY-DECISION.md
      to: config/clinic.php
      via: "exact production configuration inventory without activation"
      pattern: "production_configuration_hash"
  prohibitions:
    - requirement: GOV-05
      category: safety
      status: unverified
      flagged: true
      statement: "The application or executor MUST NOT infer or assert a legal, regulatory, professional-title, specialty, qualification, advertising, or HOF-scope conclusion on behalf of CRO-MG or qualified counsel."
    - requirement: GOV-06
      category: transparency
      status: unverified
      flagged: true
      statement: "Configured clinic values, map destinations, schema hours, contact links, or existing public promises MUST NOT be treated as proof of current real-world operations."
    - requirement: MEAS-03
      category: privacy
      status: unverified
      flagged: true
      statement: "Phase 1 MUST NOT enable analytics, emit measurement events, add consent UI, inject tags, change storage/network behavior, or treat a configured analytics identifier as permission to collect."
---

## Phase Goal

**As a** site owner, **I want to** know exactly what the site publishes and hold dated decision evidence, **so that** I can release only content that is safe and approved.

<objective>
Create the professional/legal, clinic-operations, and LGPD/analytics evidence registers with strict validators and honest pending states.

Purpose: Keep external professional, operational, and privacy decisions visible and blocking without encoding conclusions implementation cannot make.
Output: Three human-maintained records, three validators, and three focused Wave 0 Pest suites.
</objective>

<execution_context>
@/Users/jomas/.codex/gsd-core/workflows/execute-plan.md
@/Users/jomas/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-baseline-content-freeze-approval-gates/01-02-SUMMARY.md
@.planning/phases/01-baseline-content-freeze-approval-gates/01-CONTEXT.md
@.planning/phases/01-baseline-content-freeze-approval-gates/01-RESEARCH.md
@.planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
@config/clinic.php
@config/governance.php
@app/Governance/GovernanceEvidence.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register exact professional and HOF wording without inferring a legal outcome</name>
  <read_first>
    AGENTS.md
    .claude/skills/laravel-best-practices/SKILL.md
    .claude/skills/pest-testing/SKILL.md
    app/Governance/GovernanceEvidence.php
    .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
    config/clinic.php
    resources/views/partials/schema.blade.php
    resources/views/partials/procedure-schema.blade.php
  </read_first>
  <files>app/Governance/ProfessionalWordingRegister.php, .planning/phases/01-baseline-content-freeze-approval-gates/01-PROFESSIONAL-WORDING.md, tests/Unit/Governance/ProfessionalWordingRegisterTest.php</files>
  <behavior>
    - Every exact professional/qualification/specialty/HOF occurrence and every visible, metadata, and schema context is represented.
    - Scope-sensitive records require current registration evidence plus named CRO-MG/qualified legal reviewer, review date, opaque decision reference, exact decision, and current applicability.
    - RED empty/adjacency fixtures: a discovered wording item plus missing/empty register blocks; the same wording in footer and JSON-LD requires both contexts, while zero sensitive wording is valid only with complete exact-revision classification by an accountable reviewer.
    - RED encoding/status fixtures: byte-different UTF-8 wording has distinct hashes; missing, pending, rejected, quarantined, expired, stale, or context-mismatched evidence blocks with the expected stable ID/reason.
    - RED ordering/conflict fixtures: shuffled records yield stable-ID/context/reason ordered findings; duplicate full identities, conflicting scope classifications, and conflicting legal decisions block deterministically.
    - RED idempotency/read-concurrency fixture: repeated validation and two shared-read snapshots return byte-identical findings and preserve the register checksum; no scan or validator path writes the human decision.
  </behavior>
  <action>
    Search Laravel/Pest docs first with `service class json validation`, `date validation`, and `pest datasets`. Create the class and unit test with `php artisan make:class Governance/ProfessionalWordingRegister --no-interaction` and `php artisan make:test --pest --unit Governance/ProfessionalWordingRegisterTest --no-interaction`, starting with every RED fixture named above. Seed exact inventoried wording as pending and human-maintained. Record stable identity, exact wording/hash, locale, all contexts, current registration evidence locator, scope-sensitive flag, reviewer role/name, review date, opaque CRO-MG/legal decision locator, decision, applicability/recheck date, and blocked reason. Enforce deterministic stable-ID/context/reason ordering, duplicate/conflict rejection, and shared-locked snapshot reads whose repeat/concurrent-reader tests leave the register checksum unchanged. The validator enforces fields and cross-references only; it never interprets regulations or upgrades a status.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty --format agent &amp;&amp; php artisan test --compact --filter=ProfessionalWordingRegister</automated>
  </verify>
  <acceptance_criteria>
    All current professional/HOF wording is represented in every context and remains blocked until the correct current external evidence exists; no legal or clinical conclusion is generated.
  </acceptance_criteria>
  <done>D-04/GOV-05 is an explicit dated human gate rather than a repository inference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate configured clinic assertions from verified real-world operations</name>
  <read_first>
    AGENTS.md
    .claude/skills/laravel-best-practices/SKILL.md
    .claude/skills/pest-testing/SKILL.md
    app/Governance/GovernanceEvidence.php
    .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
    config/clinic.php
    resources/views/partials/footer.blade.php
    resources/views/components/site-layout.blade.php
    tests/Feature/MedicalContentTest.php
  </read_first>
  <files>app/Governance/OperationsRegister.php, .planning/phases/01-baseline-content-freeze-approval-gates/01-OPERATIONS.md, tests/Unit/Governance/OperationsRegisterTest.php</files>
  <behavior>
    - Identity, address, contact, hours, directions, accessibility, WhatsApp operations, escalation/response, and follow-up each have a current record.
    - Configured and observed values are never conflated; missing verifier/date/evidence/recheck, mismatch, staleness, duplicate, or conflict blocks.
    - RED empty/adjacency fixtures: remove each required domain in turn and expect `missing_domain`; one operational fact reused in footer/schema retains both contexts and never merges configured and observed values.
    - RED encoding/mismatch fixtures: exact-value equality still requires human observation evidence, while byte-different UTF-8 values, lossy URL normalization, stale dates, and configured/observed mismatch block with the expected domain/reason.
    - RED ordering/conflict fixtures: shuffle required and extra records and expect canonical-domain then stable-ID ordered findings; duplicate domains, duplicate full identities, conflicting observations, and conflicting statuses block deterministically.
    - RED idempotency/read-concurrency fixture: repeated validation and two shared-read snapshots return identical status/findings and preserve the register checksum; no inventory or gate path writes operational evidence.
  </behavior>
  <action>
    Search version-scoped Laravel and Pest docs before editing, create `OperationsRegister` and its unit test via `php artisan make:` with `--no-interaction`, and write every RED fixture named above first. Seed all required domains from the inventory/config as configured assertions with empty observed evidence and `pending` status. Preserve configured value, observed value, verifier, verification date, opaque evidence locator, status, recheck date, exact public contexts, and mismatch notes as separate fields. Enforce canonical domain/extra stable-ID ordering, complete domain coverage, exact identity, exact UTF-8 comparison, valid dates, freshness, duplicate and conflict detection, and shared-locked snapshot reads. Assert file checksums around repeated/shared-reader validation. Never copy a config value into observed value or convert a missing fact into public copy.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty --format agent &amp;&amp; php artisan test --compact --filter=OperationsRegister</automated>
  </verify>
  <acceptance_criteria>
    Every GOV-06 operational category is visible as verified, pending, stale, mismatched, or blocked with accountable evidence fields; config alone never satisfies the gate.
  </acceptance_criteria>
  <done>D-05/GOV-06 has a complete, dated, real-world verification register.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record the complete LGPD decision while keeping analytics disabled</name>
  <read_first>
    AGENTS.md
    .claude/skills/laravel-best-practices/SKILL.md
    .claude/skills/pest-testing/SKILL.md
    app/Governance/GovernanceEvidence.php
    .planning/phases/01-baseline-content-freeze-approval-gates/01-INVENTORY.json
    .planning/phases/01-baseline-content-freeze-approval-gates/01-BASELINE.md
    config/clinic.php
    resources/views/components/site-layout.blade.php
    resources/js/app.js
    tests/Feature/MedicalContentTest.php
  </read_first>
  <files>app/Governance/PrivacyDecision.php, .planning/phases/01-baseline-content-freeze-approval-gates/01-PRIVACY-DECISION.md, tests/Unit/Governance/PrivacyDecisionTest.php</files>
  <behavior>
    - Every MEAS-03 field is required and binds to the exact inventoried production configuration/revision.
    - Missing/blank/pending/non-current fields keep analytics and release blocked; a GA4 ID or observed script is not approval.
    - RED empty/encoding fixtures: missing/empty record and each blank required field blocks; byte-different production configuration yields a different hash without recording patient identifiers, message content, or health data.
    - RED ordering/conflict fixtures: shuffled processor/purpose/access/transfer lists normalize only by schema-defined deterministic ordering; duplicate entries, conflicting lawful bases, contradictory consent behavior, and two decisions for one release revision block with stable reasons.
    - RED production-state fixtures: configured GA4 with no observed collection remains distinct from observed collection; unknown production tags/consent state is `not_measured` with owner/reason and blocks rather than passing.
    - RED idempotency/read-concurrency fixture: repeat validation and two shared-read snapshots produce identical findings and preserve the privacy-record checksum plus public layout/JavaScript/config digests; validator and inventory runs cannot modify scripts, consent, analytics, storage, network behavior, or the human decision.
  </behavior>
  <action>
    Search Laravel/Pest docs with `configuration testing`, `json validation`, and `pest datasets` before creating the class and test with Artisan `make:` commands and `--no-interaction`; write every RED fixture named above before production logic. Seed a pending privacy record from observed analytics/configuration facts only. Require controller, processors, purposes, exact data/parameters, lawful basis, consent trigger, deny/revoke behavior, retention, access roles, transfers, production configuration and hash, approver/date, decision, unresolved questions, and evidence locators. Treat unknown production behavior as `not_measured` plus owner/reason. Enforce schema-defined list ordering, duplicate/conflict rejection, exact revision/configuration binding, and shared-locked snapshot reads. Assert record and protected-public-file digests around repeated/shared-reader validation so no validation path can modify the human decision, layout, JavaScript, config, storage, network, or analytics behavior.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty --format agent &amp;&amp; php artisan test --compact --filter=PrivacyDecision</automated>
  </verify>
  <acceptance_criteria>
    The privacy record is complete in shape, honestly pending where humans must decide, bound to exact production configuration, and unable to activate or instrument analytics.
  </acceptance_criteria>
  <done>D-06/MEAS-03 is documented as a human decision gate and analytics remains disabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public wording/config → professional and operations evidence | Existing publication is an assertion, not proof of legality or current operations. |
| deployment analytics/config → privacy decision | Observed/configured collection crosses into accountable LGPD adjudication without being activated. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-15 | Spoofing | Professional/legal decision | high | mitigate | Require current external evidence locator, authorized reviewer/date, exact wording/context, and explicit human decision. |
| T-01-16 | Tampering | Operational configured/observed values | high | mitigate | Keep fields separate, require accountable observation, and block mismatch/staleness/conflict. |
| T-01-17 | Elevation of Privilege | Analytics activation authority | high | mitigate | Validator is read-only, approval is external, and no Phase 1 file writes public analytics/config/consent behavior. |
| T-01-18 | Information Disclosure | Privacy/legal/operations evidence | high | mitigate | Store opaque locators only and emit stable IDs/reason codes rather than evidence contents. |
| T-01-19 | Repudiation | Evidence dates and applicability | medium | mitigate | Require reviewer/verifier/approver, decision date, recheck/applicability date, exact revision, and evidence locator. |
</threat_model>

<verification>
Run `vendor/bin/pint --dirty --format agent` before the three focused suites, confirm all configured assertions are pending rather than verified, confirm analytics code/config/public output was not edited, and inspect records for opaque references only.
</verification>

<success_criteria>
- D-04, D-05, and D-06 are implemented exactly and remain externally adjudicated.
- All fourteen professional/operations/privacy edge-probe cases are explicit and tested.
- No sensitive evidence payload is stored or emitted.
- Analytics remains disabled and public behavior is unchanged.
</success_criteria>

## Artifacts this phase produces

- `app/Governance/ProfessionalWordingRegister.php` — `ProfessionalWordingRegister` and GOV-05 findings.
- `.planning/phases/01-baseline-content-freeze-approval-gates/01-PROFESSIONAL-WORDING.md` — professional/HOF human decision records.
- `tests/Unit/Governance/ProfessionalWordingRegisterTest.php` — exact-context and external-evidence coverage.
- `app/Governance/OperationsRegister.php` — `OperationsRegister` and GOV-06 findings.
- `.planning/phases/01-baseline-content-freeze-approval-gates/01-OPERATIONS.md` — clinic operations observations/decisions.
- `tests/Unit/Governance/OperationsRegisterTest.php` — domain, stale/mismatch, ordering, and concurrency coverage.
- `app/Governance/PrivacyDecision.php` — `PrivacyDecision` and MEAS-03 findings.
- `.planning/phases/01-baseline-content-freeze-approval-gates/01-PRIVACY-DECISION.md` — LGPD/consent/production configuration decision.
- `tests/Unit/Governance/PrivacyDecisionTest.php` — privacy schema, disabled-state, encoding, idempotency, and concurrency coverage.

<output>
Create `.planning/phases/01-baseline-content-freeze-approval-gates/01-04-SUMMARY.md` when done.
</output>
